[phases.setup]
nixPkgs = ["nodejs-20_x", "python312"]

[phases.build]
cmds = ["cd apps/api && npm install && npm run build", "cd packages/adk-agents && pip install -r requirements.txt"]

[start]
cmd = "bash apps/api/start.sh"
